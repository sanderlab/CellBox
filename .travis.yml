language: python

python:
  - "3.6"

install:
  - pip install --upgrade pip
  - pip install pylint
  - pip install -r requirements.txt
  - cd pertbio; python setup.py install
  
script:
  - pylint pertbio
  - python scripts/main.py -config=configs/example.cfg.json
